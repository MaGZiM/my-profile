include ../mixins/mixins

doctype html
html
    head
        meta(charset="utf-8")
        title Welcome
        meta(content="width=device-width, initial-scale=1" name="viewport")
        meta(content="ie=edge" http-equiv="x-ua-compatible")

        //- favicon block start
        link(rel="shortcut icon" href="assets/img/fav_forest.png" type="image/png")
        //- favicon block end

        link(rel='stylesheet' href='/assets/css/foundation.css')
        link(rel='stylesheet' href='/assets/css/main.css')
        script(src='/assets/js/foundation.js' defer)
        script(src='/assets/js/app.js' defer)

        <!--[if lt IE 9]>
        script(src="http://cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js")
        <![endif]-->

    body
        .global
            .wrapper-index
            input.authorize-toggler#authorize-toggler(type="checkbox")
            label.authorize-button(for="authorize-toggler")
                .authorize-button__text Авторизоваться
            include ../parts/index/welcome-tab
            //-input.input-toggler#menuToggler(type="checkbox")
                label.menu-toggler(for="menuToggler")
                    span.menu-toggler__line
                    span.menu-toggler__line
                    span.menu-toggler__line
            .index__foot
                .row-bottom-writing &copy; Макс Рысухин | создано с любовью в LoftSchool | 2016
