doctype html
html
    head
        meta(charset="utf-8")
        title #{title}
        meta(content="width=device-width, initial-scale=1" name="viewport")
        meta(content="ie=edge" http-equiv="x-ua-compatible")

        //- favicon block start
        //- favicon block end

        link(rel='stylesheet' href='/assets/css/foundation.css')
        link(rel='stylesheet' href='/assets/css/main.css')
        link(rel="stylesheet" href=`/assets/css/foundation${suffix}.css${version}`)
        link(rel="stylesheet" href=`/assets/css/main${suffix}.css${version}`)

        script(src=`/assets/js/admin${suffix}.js${version}` defer)

        <!--[if lt IE 9]>
        script(src="http://cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js")
        <![endif]-->

    body
        .admin-wrapper
            img(src="assets/img/header-img.jpg").admin-wrapper-img
            .admin-page
                .admin-header
                    h1.admin-title Панель администрирования
                    a(href="/").admin-back__link
                        .admin-back Вернуться назад
                .admin-body
                    ul.admin-nav
                        li.admin-nav__tab.active-tab Skills
                        li.admin-nav__tab Posts
                        li.admin-nav__tab Works
                    ul.admin-contents
                        include ../parts/admin/contents-blog